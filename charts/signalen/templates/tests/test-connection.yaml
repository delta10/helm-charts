apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "signalen.fullname" . }}-test-connection"
  labels:
    {{- include "signalen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "signalen.frontend-legacy.fullname" . }}:80']
  restartPolicy: Never
