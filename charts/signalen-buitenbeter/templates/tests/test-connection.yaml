apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "signalen-buitenbeter.fullname" . }}-test-connection"
  labels:
    {{- include "signalen-buitenbeter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "signalen-buitenbeter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
