{{- if not .Values.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "signalen-buitenbeter.fullname" . }}
  labels:
    {{- include "signalen-buitenbeter.labels" . | nindent 4 }}
type: Opaque
data:
  JWT_TOKEN: {{ .Values.settings.jwtToken | b64enc | quote }}
{{- end }}
