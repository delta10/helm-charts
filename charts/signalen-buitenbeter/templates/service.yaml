apiVersion: v1
kind: Service
metadata:
  name: {{ include "signalen-buitenbeter.fullname" . }}
  labels:
    {{- include "signalen-buitenbeter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "signalen-buitenbeter.selectorLabels" . | nindent 4 }}
