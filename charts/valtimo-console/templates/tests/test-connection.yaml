apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "valtimo-console.fullname" . }}-test-connection"
  labels:
    {{- include "valtimo-console.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "valtimo-console.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
